

$.fn.extend {
  integrateSelect2: (selector)->
    selector = selector || '.select2'
    $(@).find(selector).select2({
      theme: "bootstrap",
      width: '100%',
      allowClear: false
    })

  integrateDatepicker: (selector)->
    selector = selector || '.datepicker'
    $(@).find(selector).datepicker({
      startView: 2,
      language: "es",
      orientation: "bottom auto",
      autoclose: true,
      clearBtn: true,
      dateFormat: "dd-mm-yyyy"
      endDate: "-4y"
      defaultViewDate: { year: 1985, month: 1, day: 1 }

    })
}


Number::format = (n, x, s, c) ->
  re = '\\d(?=(\\d{' + (x or 3) + '})+' + (if n > 0 then '\\D' else '$') + ')'
  num = @toFixed(Math.max(0, ~ ~n))
  (if c then num.replace('.', c) else num).replace new RegExp(re, 'g'), '$&' + (s or ',')

$(document).ready ->
  $('[data-toggle="tooltip"]').tooltip()
  form = $('form')
  form.integrateSelect2()
  form.integrateDatepicker()


  if ($('.people.new').length > 0)
    document.getElementById('person_phone').addEventListener 'input', (e) ->
      x = e.target.value.replace(/\D/g, '').match(/(\d{0,8})/)
      e.target.value = x[1]
      return

    document.getElementById('person_phone_code').addEventListener 'input', (e) ->
      x = e.target.value.replace(/\D/g, '').match(/(\d{0,4})/)
      e.target.value = x[1]
      return

    document.getElementById('person_dni').addEventListener 'input', (e) ->
      x = e.target.value.replace(/\D/g, '')
      e.target.value = x
      return
  return



$(document).on 'turbolinks:load', ->
  $('[data-toggle="tooltip"]').tooltip()
  datatable = $('.datatable')
  datatable.DataTable
    'language': 'url': '//cdn.datatables.net/plug-ins/9dcbecd42ad/i18n/Spanish.json'
    'bPaginate': true
    'info': false
    'responsive': true
    'deferRender': true
    'stateSave': true

  form = $('form')
  form.integrateSelect2()
  form.integrateDatepicker()


  if ($('.people.new').length > 0)
    document.getElementById('person_phone').addEventListener 'input', (e) ->
      x = e.target.value.replace(/\D/g, '').match(/(\d{0,8})/)
      e.target.value = x[1]
      return

    document.getElementById('person_phone_code').addEventListener 'input', (e) ->
      x = e.target.value.replace(/\D/g, '').match(/(\d{3,4})/)
      e.target.value = x[1]
      return

    document.getElementById('person_dni').addEventListener 'input', (e) ->
      x = e.target.value.replace(/\D/g, '')
      e.target.value = x
      return

    document.getElementById('person_address_floor').addEventListener 'input', (e) ->
      x = e.target.value.replace(/\D/g, '').match(/(\d{0,1})/)
      e.target.value = x[1]
      return

    document.getElementById('person_address_number').addEventListener 'input', (e) ->
      x = e.target.value.replace(/\D/g, '').match(/(\d{0,6})/)
      e.target.value = x[1]
      return

$(document).on 'turbolinks:before-cache', ->
    dataTable =  $($.fn.dataTable.tables(true)).DataTable()
    if (dataTable != null)
      dataTable.destroy();


$(document).on 'turbolinks:load', ->
  $('#people-datatable').dataTable
    processing: true
    serverSide: true
    ajax:
      url: $('#people-datatable').data('source')
    pagingType: 'full_numbers'
    language: 'url': '//cdn.datatables.net/plug-ins/9dcbecd42ad/i18n/Spanish.json'
    bPaginate: true
    info: false
    responsive: true
    deferRender: true
    stateSave: true
    columns: [
      {data: 'locality'}
      {data: 'population_group'}
      {data: 'dni'}
      {data: 'firstname'}
      {data: 'lastname'}
      {data: 'options'}

    ]
