<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container">
        <a class="navbar-brand" href="/"><%= image_tag "logo_vacunate.png", style:"max-width:120px;"%></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample07" aria-controls="navbarsExample07" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarsExample07">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
              <a class="nav-link" href="/">Inicio <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/people/new">Formulario</a>
            </li>
            <% if user_signed_in? %>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="http://example.com" id="dropdown07" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Administración</a>
              <div class="dropdown-menu" aria-labelledby="dropdown07">
                <a class="dropdown-item" href="/people">Inscripciones</a>
                <a class="dropdown-item" href="/tablero">Tablero</a>
                <a id="createCSV" class="dropdown-item" href="">Generar CSV</a>
                <%= link_to "Descargar P/Excel", download_excel_path, class: "dropdown-item", method: :post %>
                <%= link_to "Descargar P/Wps", download_wps_path, class: "dropdown-item", method: :post %>
              </div>
            </li>
            <% end %>
          </ul>
          <% if user_signed_in? %>
          <ul class="navbar-nav">
            <li class="nav-item">
              <% if !current_user.area.nil? %>
              <%= link_to current_user.area.upcase, "#", :class => "nav-link" %>
              <% end %>
            </li>
            <li class="nav-item active">
              <%= link_to "Salir", destroy_user_session_path, :method => :delete, :class => "nav-link" %>
            </li>
          </ul>
          <% end %>
        </div>
      </div>
    </nav>

<script type="text/javascript">
  $("#createCSV").on("click", createCSV);
  $("#sendCSV").on("click", sendCSV);

  function createCSV(){
    $.ajax({
      url: '/create_csv',
      type: 'POST'
    }).done(function(data) {
      console.log("Se creo el archivo");
    }).fail(function(xhr, status, error) {
      console.log("Fallo la carga");
    });
  }

  function sendCSV(){
    $.ajax({
      url: '/send_csv',
      type: 'POST'
    }).done(function(data) {
      console.log("Se creo el archivo");
    }).fail(function(xhr, status, error) {
      console.log("Fallo la carga");
    });
  }

</script>