<%= form_with(model: population_group, local: true) do |form| %>
  <% if population_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(population_group.errors.count, "error") %> prohibited this population_group from being saved:</h2>

      <ul>
      <% population_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code %>
    <%= form.number_field :code %>
  </div>

  <div class="field">
    <%= form.label :priority %>
    <%= form.number_field :priority %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
